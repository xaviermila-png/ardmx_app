import 'package:flutter/material.dart';

import '../../widgets/app_scaffold.dart';

/// Placeholder — built in phase 5 (Scene/Channels + RGB Wheel).
class SceneChannelsScreen extends StatelessWidget {
  const SceneChannelsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const AppScaffold(
      title: 'Escena / Canals',
      body: Center(child: Text('Pantalla pendent (fase 5)')),
    );
  }
}
