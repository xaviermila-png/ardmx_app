import 'package:flutter/material.dart';

import '../../widgets/app_scaffold.dart';

/// Placeholder — built in phase 6 (Cycle Programming).
class CycleProgrammingScreen extends StatelessWidget {
  const CycleProgrammingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const AppScaffold(
      title: 'Programació de Cicles',
      body: Center(child: Text('Pantalla pendent (fase 6)')),
    );
  }
}
